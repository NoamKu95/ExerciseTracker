export enum ButtonType {
  PRIMARY,
  SECONDARY,
  TEXT,
}

export enum AppStateStatus {
  ACTIVE = 'active',
  BACKGROUND = 'background',
  INACTIVE = 'inactive',
}

export enum DayPeriod {
  MORNING,
  NOON,
  EVENING,
}
