export enum ButtonType {
  PRIMARY,
  SECONDARY,
  TEXT,
}

export enum AppStateStatus {
  ACTIVE = 'active',
  BACKGROUND = 'background',
  INACTIVE = 'inactive',
}

export enum DayPeriod {
  MORNING = 'morning',
  NOON = 'noon',
  EVENING = 'evening',
  NIGHT = 'night',
}

export enum Gender {
  FEMALE = 'female',
  MALE = 'male',
  OTHER = 'other',
}
